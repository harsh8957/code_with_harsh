#include <bits/stdc++.h>
using namespace std;

void optimizeIO() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
}

int main() {
    optimizeIO();
    int t;
    cin >> t;
    while (t--) {
        int a,b;
        cin>>a>>b;
        int ans = a+b;
        cout<<ans<<endl;
        //here we are just adding 2 numbers for harsh pata nhi kyu pagal hai sala 
        //ok
     
      
      
      
    }
    return 0;
}
